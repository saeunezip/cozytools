# 스타소울 파견 계산기 — 설계 확정 (v3 / 최종, 2026-08-23 버그 정정 반영)

Claude Code(PC 로컬)에서 이 기능을 만들 때 참고할 설계 문서. Cowork에서 새우님과 논의해서 정리함 (2026-08-22).
대상 프로젝트는 `guild-flower`가 아니라 **`cozytools`** (형제 폴더 `C:\Users\kyung\Documents\cozytools`, 리포 `saeunezip/cozytools`). 배포는 현재 GitHub Pages(`main` 브랜치, `cozytools.kro.kr`) — Cloudflare Pages로 이전 계획은 있지만 아직 전환 전이니 실제 배포 상태는 구현 시점에 다시 확인할 것. 기존 `calc.html`·`calendar.html`과 같은 **정적 HTML/JS** 패턴.

> **v3 개정 (2026-08-22) — 최종 확정본.**
> **가장 큰 변경은 획득 모델의 성격**: 하루 12/30개는 "자정에 지급되는 양"이 아니라 **하루 동안 수확해서 얻을 수 있는 상한(캡)**이고, 자정에 초기화되는 건 지급이 아니라 **이 상한**이다. 로테이션 변경도 **같은 자정에 동시에** 일어난다. 따라서 v2에서 "확정 수치"로 다뤘던 **확보 가능 총량은 전부 "최대(상한) 기준 이론치"로 격을 낮췄다** — 유저가 깜빡하거나 사정이 있으면 상한을 못 채울 수 있기 때문이다.
> 그 밖에: **혼합 심기 미지원 확정**, 캘린더 카테고리 필드 **존재 확인**(필터 조회 가능 여부·스키마는 구현 시 실측), 가속 계산 보류 유지.
>
> **확정 확인(v3 확정 이후 추가 결정, 2026-08-22)**: ① 보너스 꽃 **썸네일은 dorazy.art 핫링크**로 확정(자체 다운로드/커밋 안 함). ② 시즌 꽃 데이터 입력은 **`register.html`에 새 등록창을 만들어** 새우님이 직접 등록하는 방식으로 확정(기존 "일정 추가" 폼과 같은 자리, `cozytools-worker` 연동) — `seasons.json` 직접 편집이 주 경로가 아니게 됨.
>
> *(v2 개정 이력: 소울 획득 모델을 "등급별 획득 비율 `p`(2:5:5)"에서 "짝꿍 꽃 단일 등급 집중 재배"로 전면 교체, 슬롯 개수 입력화, 배치 크기 1~12 자유화, 전 시즌 소울 규칙 확정, 캘린더 연동 검토 추가.)*
>
> **🐛 버그 정정 (2026-08-23)**: `starsoul.html` 구현 중 새우님이 "SSR 12마리 파견했는데 24시간이 아니라 2시간으로 나온다"고 발견. 원인은 "소요시간" 표를 배치 전체 완료시간으로 잘못 읽어서(사실은 **마리당** 시간) — 코드와 문서 둘 다 고침. 영향받은 계산: 보너스 마감 시각(파견 시스템 절), 핵심 계산 결과 3·4·5번, 시뮬레이션 로직. "하루 파견 갱신 횟수 `k`" 입력은 실제 구현에서 빠졌음(이벤트 기반 시뮬레이션으로 대체 — 3단계 참고).

---

## 목적 / 배경

더 코지 플로리스트의 **스타소울 파견**은 "지금 가진 소울을 언제 파견 걸어야 코인을 제일 많이 먹는가"가 핵심인데, 규칙이 여러 겹으로 얽혀 있어서 머리로 계산하기 어렵다:

- 보너스 소울이 **매주 바뀌고**, 배율은 **파견 시작이 아니라 완료 시점** 기준으로 판정된다
- 슬롯 개수(2~5)와 등급별 소요시간에 따라 하루에 굴릴 수 있는 양이 달라진다
- 소울은 짝꿍 꽃을 심어서 얻는데, 하루 획득 상한(12~34개)이 슬롯 용량과 맞물리는 지점이 사람마다 다르다
- **전 시즌 소울은 보너스를 절대 못 받는다** — 쟁여두기 전략에 시한이 걸려 있다

이 계산기는 길드원(그리고 커뮤니티 일반 유저)이:

1. 이번 주 보너스가 뭔지, 다음 로테이션까지 얼마나 남았는지, **보너스 마감 시각이 언제인지** 한눈에 보고
2. **지금 내 보유량을 직접 입력**해서, 이번 로테이션이 끝날 때까지 최대 몇 마리를 굴릴 수 있고 코인이 얼마나 들어올지

를 바로 확인할 수 있게 해주는 도구다.

`범용도구사이트-구상.md`의 "범용 도구" 조건을 그대로 따른다 — **개인 스프레드시트/게임 계정 연동 없이, 사용자가 자기 숫자를 직접 입력해서 바로 써볼 수 있어야 함.**

### 이번 v1의 범위 (여기서 벗어나지 말 것)

- ✅ 현재 보너스 로테이션 표시 + 다음 로테이션까지 카운트다운 + 보너스 마감 카운트다운
- ✅ 보유량 입력 → 이번 로테이션 종료까지의 **최대** 획득/파견 가능량 및 예상 코인 계산
- ❌ **상점 / 코인 소비 계산은 v1 범위 밖.** 상점 시즌은 종료일이 따로 있고(대략 150일로 훨씬 김) 파견 시즌(28일)과 주기가 아예 달라서, 별도 설계로 뺀다.
- ❌ **가속(다이아) 계산은 v2로 미룸.** 규칙은 확정됐지만 계산기 범위는 v1에서 넓히지 않는다. (새우님 확인: "염두에 두고 있지만 일단 보류")
- ❌ **혼합 심기(여러 등급 동시 재배)는 지원하지 않는다. (확정)** 정밀 확률 모델을 만들려 하지 말 것 — 독립 시행 편차가 커서 정밀도가 안 나온다. 화면에 전제만 명시한다.

---

## 확정된 게임 규칙

새우님이 확인해준 내용을 전부 반영한 확정본. **아래는 전부 확정 사실이고, ⚠️ 표시가 붙은 곳만 미확정이다.**

### 시즌 · 보너스 로테이션

- 시즌은 **28일(4주)** 단위. **이번 시즌 시작일은 2026-08-23(일)** → 종료 2026-09-20(일) 00:00.
- 시즌 시작 시 **짝꿍 꽃 12종이 고정**으로 정해진다: **SSR 2 / SR 5 / R 5**. 이 12종이 곧 그 시즌의 소울 종류다.
- 이 12개 중 **3개씩 묶여 "보너스 소울"**로 지정되고, **매주 변경**. 4주 동안 4세트가 순환한다.
- **로테이션 경계 = 토요일→일요일로 넘어가는 자정, `일요일 00:00 KST`. (확정)**
- 시즌 시작일도 일요일이므로 **시즌 경계와 주간 경계가 항상 일치한다.** 주차 계산이 단순해진다:
  ```
  주차 = floor((현재시각 − 시즌시작 00:00 KST) / 7일) + 1
  ```
- **보너스 배율은 정확히 2배 고정. 등급별 차등 없음. (확정)**
- **게임은 다음 주 로테이션을 미리 보여주지 않는다. (확정)** → 계산기는 미래 보너스를 절대 예측하면 안 된다.

> **보너스 3개 조합에 대한 경험적 관찰 (⚠️ 확정 규칙 아님)**
> 지금까지 관찰된 조합은 **`SSR 1 + SR 1 + R 1`** 또는 **`SR 1 + R 2`** 두 가지뿐이고, SSR 2개가 한 주에 같이 보너스로 나온 적은 아직 없다. 결과적으로 한 시즌에 SSR 보너스 주는 많아야 2주, 아예 없을 수도 있다.
> **이건 게임이 보장한 규칙이 아니라 관찰된 패턴일 뿐이다.** 계산기가 이 패턴으로 다음 주를 추측하는 기능은 v1에 넣지 않는다. 다만 데이터 구조는 **과거 로테이션이 그대로 누적되게** 짜둘 것 — 나중에 "역대 로테이션 기록" 콘텐츠로 확장할 수 있게 (→ 데이터 구조 절).

### 소울 획득 — 상한(캡) 모델

**소울은 "짝꿍 꽃"을 심어서 수확할 때 나온다.** 시즌 시작 시 정해지는 12종이 바로 이 짝꿍 꽃이다.

- 짝꿍 꽃을 수확하면 **그 꽃 등급의 소울이 낮은 확률로** 나온다. 매 수확이 확률적이라 "나오냐 안 나오냐"가 갈릴 뿐, **나오면 심은 꽃의 등급 그대로 나온다.**
- 즉 **등급이 섞여서 배분되는 게 아니다.** SSR 짝꿍 꽃만 100% 심으면 나오는 소울도 100% SSR이다.
- 두 종류 이상을 나눠 심으면 **정확히 비율대로 나뉘지 않는다** — 각각 낮은 확률의 독립 시행이라 편차가 크다. (2종을 반씩 심어도 15:15로 안 나옴)

> **→ v1 계산 모델의 기본 가정 (확정)**
> 이 계산기를 쓰는 유저는 보통 **"이번엔 SSR 노리고 파겠다"처럼 한 등급에 집중해서 심는다.**
> 그러면 **하루 획득 상한(무료 12 / 패스권 30)은 사실상 전부 계산 대상 등급**이다.
> **등급별 획득 비율 파라미터 `p`는 존재하지 않는다.** 곱하지 말고, 입력받지도 말 것.

| 경로 | 하루 획득 **상한** | 성격 |
|---|---|---|
| 무료 | 12개 | **상한 확정** — 전량 집중 재배 등급 |
| 유료 패스권 (7,500원) | 30개 | **상한 확정** — 전량 집중 재배 등급 |
| 친구 | 최대 4개 | **추정** (아래) |

> **⚠️ 12/30은 "지급량"이 아니라 "획득 상한"이다. (확정 — v3 정정)**
> 자정에 소울을 12개(또는 30개) **받는 게 아니다.** 하루 동안 수확을 통해 **최대 그만큼까지 얻을 수 있다**는 캡이고, **자정 00:00 KST에 이 캡이 초기화된다.**
> 실제로 그만큼 얻으려면 유저가 그 시간 안에 직접 수확해야 하고, 깜빡하거나 사정이 있으면 상한보다 적게 얻는다.
> **→ 계산기가 내놓는 획득량은 전부 "이론 최대치"다. "확정 획득"이라고 표현하면 안 된다.**
>
> **캡 초기화와 로테이션(보너스) 변경은 같은 자정에 동시에 일어난다. (확정)**
> "지급"이라는 별도의 이산 이벤트가 없으므로, **"지급이 먼저냐 로테이션이 먼저냐"는 질문 자체가 성립하지 않는다.** 자정 경계 처리 코드는 하나로 묶으면 된다.

**친구 소울 (추정 유지)**
친구가 심은 짝꿍 꽃 등급을 보고 유저가 **받을지 말지 고를 수 있다** — 완전 랜덤은 아니다. 하지만 어떤 친구가 뭘 심었는지를 계산기가 알 방법이 없으므로 여전히 **추정 영역으로 분리**한다.
입력 문구는 현실에 맞게: **`"목표 등급을 심은 친구에게서 하루 몇 개나 확실히 받을 수 있나요? (0~4)"`**
**과설계하지 말 것** — 친구별 재배 등급 입력 같은 건 v1에 넣지 않는다. 슬라이더 하나면 충분하다.

### 파견 시스템

- **슬롯은 기본 2개, 확장으로 최대 5개. (확정)** 확장 비용이 비싸지 않아 대부분 5개까지 확장한 것으로 보임.
  → **슬롯 개수를 상수 5로 박지 말고 사용자 입력값(2~5, 기본 5)으로 둘 것.**
- 한 슬롯에 한 번 파견으로 **1~12마리 자유롭게** 넣을 수 있다. **12는 상한일 뿐 정확히 12일 필요가 없다. (확정)**
- **한 배치는 등급이 단일해야 한다** — SSR/SR/R을 한 슬롯에 섞을 수 없다.

| 등급 | 코인/마리 | 소요시간(마리당) | 보너스 시 코인/마리 |
|---|---|---|---|
| SSR | 4 | 2시간 | 8 |
| SR | 3 | 1.5시간 | 6 |
| R | 2 | 1시간 | 4 |

> **⚠️ "소요시간"은 배치 전체 완료 시간이 아니라 마리 1개당 시간이다. (확정 — 2026-08-23 정정)**
> 배치 완료 시간 = **마리 수 × 마리당 소요시간**. 예: SSR 12마리 배치 = 2시간 × 12 = **24시간**, 1마리만 보내면 2시간.
> (v3 문서 초판에서 이걸 "배치 하나가 통째로 2시간 만에 끝난다"로 잘못 구현해서 실제 코드에 버그가 났었음 — 2026-08-23 새우님이 발견, `starsoul.html`도 같이 수정함. 아래 "핵심 계산 결과"와 "시뮬레이션 로직" 절은 이 정정을 반영해 다시 계산한 것.)

- **보너스 배율은 "파견 완료 시점" 기준으로 판정된다. (확정)** 시작할 때 보너스가 아니었어도 완료 시점에 보너스면 2배가 붙고, 반대로 시작할 땐 보너스였는데 완료 시점에 풀렸으면 1배다.
- **전 시즌 소울은 파견은 되지만 보너스 대상이 절대 될 수 없다. (확정)** 보너스는 그 시즌의 짝꿍 꽃 12종에서 나온 소울만 해당한다. → **시즌 경계를 넘겨 쟁여둔 소울은 2배 기회를 영구히 잃는다.** 계산기가 반드시 경고해야 할 지점.
- **파견 도중 취소는 불가능. (확정)**
- **가속은 5분당 1다이아, 부분 가속 없이 무조건 즉시 완료(전액 스킵)만 가능. (확정)** v1은 가속을 쓰지 않는다고 가정한다. 다만 **가속하면 완료 시각이 앞당겨져 보너스 판정이 바뀔 수 있다**는 건 확정 사실이므로, v2 기능 후보로 메모해 둔다 (→ 구현 메모).

### 플레이어들의 실제 전략 (계산기가 뒷받침해줘야 할 것)

- **스톡 전략**: SSR이 보너스가 아닌 주에는 SSR을 덜 파견하고 쟁여뒀다가, SSR 보너스 주간(7일) 동안 슬롯을 끊김없이 풀가동해서 2배를 최대한 뽑아낸다. **단 시즌을 넘기면 그 재고는 영영 보너스를 못 받는다.**
- **타이밍 트릭 (도박성)**: 보너스가 바뀌는 경계에 맞춰 소울을 미리 걸어두면 그 파견의 **완료 시점이 새 보너스 주간으로 넘어간다.** 다음 주도 그 등급이 보너스면 사실상 하루치를 더 버는 효과. 아니면 그냥 정상 처리라 손해는 없다. 대신 **매주 일요일 자정에 맞춰 파견을 걸어야 하는 실행 난이도**가 있다.

---

## 핵심 계산 결과 (설계 중 도출 — 계산기 UI에 반영할 것)

`임무횟수계산기-설계.md`의 "고급 새로고침은 항상 손해다"에 해당하는, 이번 계산기의 결론들.

### 1. 등급별 슬롯 시간당 효율이 정확히 똑같다

```
SSR: 4코인 ÷ 2h   = 2 코인 / (시간·마리)
SR : 3코인 ÷ 1.5h = 2 코인 / (시간·마리)
R  : 2코인 ÷ 1h   = 2 코인 / (시간·마리)
```

슬롯 풀가동 시 **어느 등급이든 슬롯 1개당 시간당 24코인**(12마리 × 2), 5슬롯이면 **120코인/시간**(보너스 240)으로 완전히 동일하다.
→ **"슬롯 시간을 아끼려고 어떤 등급을 고르는" 최적화는 이 게임에서 의미가 없다.** 계산기가 "R을 돌려야 회전이 빨라서 이득" 같은 결론을 내면 그건 틀린 설계다.

### 2. 배치를 어떻게 쪼개든 코인 총량은 같다

배치 크기가 1~12 자유가 되면서 "어떻게 나눠 담을까"가 고민거리처럼 보이지만, 코인은 **마리 수 × 등급 코인 × 완료 시점 배율**로만 결정된다. 20마리를 `12+8`로 넣든 `4×5`로 넣든 코인은 동일하다.
→ **계산기는 배치 분할을 최적화하려 들 필요가 없다. 오직 "몇 마리를, 언제 완료되게" 넣느냐만 계산하면 된다.**
(단 슬롯 점유 시간은 달라지므로, 경계 직전에 슬롯을 비워두고 싶을 때만 분할이 의미 있다 — 아래 4번)

### 3. 슬롯 개수와 등급에 따라 병목이 바뀐다 (2026-08-23 정정 — 아래 "소요시간은 마리당" 정정 반영)

> 이전 판은 "하루 파견 횟수 `k`"라는 입력을 가정하고 `슬롯 s × 12 × k`로 계산했는데, 실제 구현(`starsoul.html`)은 그 입력 없이 **슬롯이 비면 즉시 재고가 있는 만큼 바로 채워진다고 가정하는 이벤트 기반 시뮬레이션**으로 만들어졌다(이게 "이론 최대치" 취지에 더 맞음 — → 구현 메모). 아래는 그 방식에 맞춘 재계산이다.

소요시간이 **마리당**이므로, 슬롯 하나가 남은 시간 `T`(시간) 동안 처리할 수 있는 이론 최대량은 `T ÷ 마리당 소요시간`이고, 슬롯이 `s`개면 그 `s`배다. 등급마다 마리당 시간이 다르므로 **병목 지점도 등급마다 다르다.**

예: 로테이션이 막 시작돼 `T = 168시간(7일)` 전부 남아있다고 하면:

| 등급 | 마리당 시간 | 슬롯 1개 7일 처리량 | 슬롯 5개 | 슬롯 3개 | 슬롯 2개 |
|---|---|---|---|---|---|
| SSR | 2h | 84마리 | 420마리 | 252마리 | 168마리 |
| SR | 1.5h | 112마리 | 560마리 | 336마리 | 224마리 |
| R | 1h | 168마리 | 840마리 | 504마리 | 336마리 |

일주일 획득 상한(패스권 집중 재배 기준)은 최대 `30×7 = 210마리`, 친구 소울까지 더해도 `~238마리`.
→ **SSR을 슬롯 2개로 굴리는 경우만(168 < 210) 슬롯이 실제 병목이다.** 슬롯 3개부터는(252 > 210) 여유가 생기고, 등급이 낮을수록(소요시간이 짧을수록) 슬롯 처리량이 커서 병목이 걸릴 일이 더 줄어든다.
→ **결론은 이전과 같다: 슬롯 개수는 입력값이어야 한다.** 다만 "몇 번 로그인하냐"가 아니라 "등급별 마리당 시간 × 남은 기간"으로 계산해야 맞다.

### 4. 유일하게 의미 있는 최적화 = 보너스 적용률

같은 SSR 1마리라도 비보너스 주에 쓰면 4코인, 보너스 주에 쓰면 8코인. **정확히 2배.**

```
스톡 이득 = 쟁여둔 마리 수 × 등급 기본 코인 × 1
```

예: SSR 630마리를 비보너스 주에 태우면 2,520코인, 보너스 주로 미루면 5,040코인 → **+2,520코인**.

여기서 나오는 실행 규칙이 하나 있다. 완료 시점 판정이므로 **보너스 마감 시각**이 존재한다:

```
보너스 마감 시각(12마리 꽉 채운 배치 기준) = 로테이션 경계 − (마리당 소요시간 × 12)
  SSR → 24시간 전 = 토요일 00:00 KST
  SR  → 18시간 전 = 토요일 06:00 KST
  R   → 12시간 전 = 토요일 12:00 KST
```

> **(2026-08-23 정정)** 이전 판은 "소요시간"을 배치당으로 잘못 계산해서 SSR 22:00/SR 22:30/R 23:00로 훨씬 늦게 잡혀 있었다. 실제로는 마리 수만큼 시간이 늘어나므로 **12마리 꽉 채운 배치 기준 마감은 훨씬 이르다**(SSR은 무려 토요일 자정, 즉 로테이션 경계 하루 전). 이건 "12마리를 새로 걸어도 안전한" 가장 보수적인 기준이고, **더 적은 마리 수를 걸 거면 이 시각 이후에도 완료가 경계 전일 수 있다** — 정확한 값은 실제 마리 수까지 반영하는 시뮬레이션(→ 3단계)이 계산한다.

- **보너스 주에는 마감 시각 전까지 넣을 수 있는 만큼 다 넣어라.** 마감 후에 거는 건 확정 2배를 포기하고 도박하는 것이다.
- **마감 전에 다 채우고도 재고가 남으면**, 남는 분은 경계를 넘겨 완료되도록 거는 게 맞다 (다음 주도 보너스면 이득, 아니면 손해 없음). 이게 새우님이 말한 트릭의 정확한 형태다.
- **비보너스 주 말미**에는 어차피 1배라, 완료를 다음 주로 넘기는 게 무손실 도박이다.

계산기는 현재 시각과 경계를 알고 있으므로 **마감까지 남은 시간을 카운트다운으로 띄울 수 있다.** 이게 이 도구에서 가장 실용적인 화면 요소다.

### 5. 쟁여둔 재고를 보너스 주 안에 다 태울 수 있는가 (시즌 시한 포함)

전 시즌 소울은 보너스를 못 받으므로, 스톡은 **반드시 그 시즌 안의 보너스 주에 소진**돼야 한다.

```
보너스 주 1주(7일=168h) 소진 용량 = s × (168 ÷ (마리당 소요시간 × 12)를 올림 + 1) × 12   ← 12마리 꽉 채운 배치를 반복한다고 가정
```

등급마다 마리당 시간이 달라서 소진 용량도 등급마다 다르다. **아래는 SSR(가장 오래 걸리는 등급) 기준** — SR·R은 이보다 여유롭다:

| 재고 (SSR 기준) | `s=5`(주간 480마리) | `s=3`(주간 288마리) | `s=2`(주간 192마리) |
|---|---|---|---|
| 420마리 | 1주 | 2주 | 3주 |
| 630마리 (3주 쟁여둠, 패스권 상한 기준) | 2주 | 3주 | 4주 |
| 840마리 (시즌 전체, 패스권 상한 기준) | 2주 | 3주 | 5주 |

> **(2026-08-23 정정)** 이전 판은 "소요시간"을 배치당(2시간 등)으로 잘못 계산해서 훨씬 낙관적인 수치가 나왔었다. 마리당 시간으로 고치니 SSR은 실제로 슬롯을 상당히 오래 붙잡아둔다(12마리 배치 = 24시간) — 슬롯이 적을수록 소진에 여러 주가 걸릴 수 있다는 게 더 뚜렷해졌다. SR(마리당 1.5h)·R(마리당 1h)은 같은 재고라도 더 빨리 소진된다.
→ **슬롯 5개면 SSR도 재고 420마리를 한 주에 소화한다.** 슬롯이 적으면(특히 2개) 여러 주에 걸쳐 소진해야 할 수 있고, 그만큼 "그 등급 보너스 주가 이번 시즌에 몇 번이나 올지"가 더 중요해진다.
→ 위 재고 수치는 **상한을 매일 꽉 채웠을 때의 이론 최대치**다. 실제 재고는 이보다 작을 가능성이 높고, 그만큼 여유가 생긴다.
→ 그리고 **그 시즌에 해당 등급 보너스 주가 아예 없을 수도 있다**(경험적 패턴상 SSR은 0~2주). 다음 주 로테이션을 미리 볼 수 없으므로 이건 계산기가 예측할 수 없는 리스크다. **경고 문구로만 알릴 것.**

---

## 데이터 / 계산 로직

### 입력값

| 항목 | 기본값 | 비고 |
|---|---|---|
| 계산 대상 등급 | SSR | SSR / SR / R 중 하나 (탭) |
| 현재 보유 수량 `N₀` | 0 | 사용자 직접 입력 |
| 획득 경로 | 무료(상한 12) / 패스권(상한 30) | 토글 |
| **슬롯 개수 `s`** | **5** | **2~5. 상수로 박지 말 것** |
| **오늘 남은 획득 가능 개수** | 0 | 0~상한. **"지급받았나"가 아니라 "오늘 캡이 얼마나 남았나"** |
| 하루 상한 달성률 (고급, 선택) | 100% | 매일 캡을 다 못 채우는 유저용. 100%면 결과가 곧 이론 최대치 |
| 친구 소울 | 0 | 0~4. "목표 등급을 심은 친구에게서 확실히 받을 수 있는 수". **추정 영역에만 반영** |

화면에 전제를 명시한다:

> 이 계산은 **짝꿍 꽃을 계산 대상 등급 한 종류로만 집중해서 심고 있다**고 가정합니다.
> 여러 등급을 섞어 심으면 각각 낮은 확률의 독립 시행이라 결과 편차가 커져서 정확한 예측이 어렵습니다. (혼합 심기 미지원)
> 하루 12/30개는 **획득 상한**이라, 아래 결과는 매일 상한을 다 채웠을 때의 **이론 최대치**입니다.

### 1단계: 남은 시간 계산

```
경계시각 = 지금 이후 첫 번째 "일요일 00:00 KST"
남은 시간 T = 경계시각 − 현재시각
남은 캡 초기화 횟수 D = 경계시각 전까지 도래하는 00:00 KST 의 개수 (경계 자정 자체는 제외)
보너스 마감 시각(12마리 꽉 채운 배치 기준) = 경계시각 − (등급별 마리당 소요시간 × 12)
시즌 종료시각 = 시즌시작 + 28일 (일요일 00:00 KST)
현재 주차 = floor((현재시각 − 시즌시작) / 7일) + 1
```

- **KST(UTC+9) 고정 오프셋**으로 내부 계산하고, **표시는 사용자 로컬 시간 + "(KST 기준)" 라벨**을 같이 보여준다. KST는 서머타임이 없어서 고정 오프셋이 안전하다.
- 예: 지금이 토요일 10:00이면 `D = 0` (경계 전에 자정이 더 안 옴 → 오늘 남은 캡만 계산). 수요일이면 목·금·토 = `D = 3`.
- **캡 초기화와 로테이션 변경이 같은 자정이므로, 자정 처리 로직은 하나면 된다.**

### 2단계: 최대 확보 가능량 (상한 기준 이론치)

```
최대 추가 획득 = 오늘 남은 획득 가능 개수 + (일일 상한 × 달성률 × D)
최대 확보 가능 총량 = N₀ + 최대 추가 획득     ← "이론 최대치(상한)"
친구 추정        = 친구소울/일 × (D + 1)      ← "추정" 수치, 별도 표시
```

> **이건 확정 수치가 아니다.** 하루 12/30개는 캡이고, 실제로 그만큼 얻으려면 유저가 그 시간 안에 직접 수확해야 한다. 깜빡하거나 사정이 있으면 이보다 적게 나온다.
> **UI 라벨은 반드시 "최대 확보 가능"·"이론 최대치"로 쓸 것. "확정 획득"이라는 표현을 쓰지 말 것.**
> 계산식 자체는 위 그대로 두고 라벨만 정확하게 붙이면 된다. 달성률을 100% 미만으로 넣으면 그만큼 현실적인 추정치가 된다.

> 친구 소울은 유저가 등급을 보고 고를 수 있으므로 완전 랜덤은 아니지만, 매일 그 등급을 심은 친구가 있으리란 보장이 없다. UI에서 **"참고용 추정치"**를 반드시 붙이고 본 계산 합계에 더하지 말 것.

### 3단계: 파견 시뮬레이션

> **(2026-08-23 갱신)** 아래는 실제 `starsoul.html` 구현과 일치하도록 다시 쓴 버전. 이전 판의 "하루 활동시간을 `k`등분한 시각마다 확인" 방식 대신, **이벤트 기반**(소울 도착·슬롯 완료 시점에만 계산)으로 만들어졌다 — "슬롯이 비면 실제로는 언제 로그인하느냐와 무관하게, 재고가 있는 즉시 채워진다"고 가정하는 게 "이론 최대치"라는 목적에 더 맞고, `k` 입력 없이도 구현이 더 간단해진다. **배치 완료시각 계산도 정정됐다 — 마리당 소요시간 × 마리 수.**

```
초기: stock = N₀, 슬롯배열[s개] = 전부 "지금 비어 있음"
도착 이벤트 목록 = [ (지금, N₀ + 오늘 남은 획득 가능 개수) ] + [ (남은 각 자정, 일일 상한 × 달성률) 매일 하나씩 ]

이벤트를 시간순으로 처리 (도착 이벤트 시각 + 슬롯이 빌 것으로 예정된 시각들을 합친 큐):
  각 이벤트 시각 t 에서:
    1. t 가 도착 이벤트면 stock += 그 양
    2. 완료시각 ≤ t 인 슬롯을 비운다
    3. 빈 슬롯마다: stock > 0 인 동안 min(12, stock)만큼 채운다
       ※ 몇 마리를 넣든 코인 총량엔 영향 없음(핵심 결과 2번) — 그냥 가능한 만큼 채우면 됨
    4. 그 배치의 완료시각 = t + (등급별 마리당 소요시간 × 이번에 넣은 마리 수)   ← 마리 수만큼 곱하는 게 핵심
       완료시각 기준으로 배율 판정:
         - 완료시각 <  경계시각 → 이번 주 보너스 여부로 판정 (확정)
         - 완료시각 >= 경계시각 → 다음 주는 알 수 없음 → "경계 넘김" 항목으로 분리
    5. 코인을 배율별 버킷에 누적, 그 완료시각을 새 이벤트로 큐에 추가

종료: 처리할 이벤트가 더 없거나(재고 소진 + 모든 슬롯 빔), 이벤트 시각이 경계시각을 넘으면 중단
```

**유휴 시간을 "최적화"하지 않는다.** 슬롯 여유가 있는 조합에서는(핵심 결과 3번 — 이제 등급마다 기준이 다름) 시뮬레이션이 유휴를 없애려 들지 말고 그냥 **가동률을 리포트**한다.

```
이론 용량 C = 슬롯 s개가 남은 시간 T 동안 처리 가능한 최대치
            ≈ s × (12마리 꽉 채운 배치를 반복한다고 가정했을 때의 사이클 수) × 12   ← 핵심 결과 3번 공식과 동일
슬롯 가동률 = 실제 파견 마리 수 / C
이월 재고 = 최대 확보 가능 총량 − 실제 파견 마리 수
```

### 4단계: 출력

| 출력 | 성격 |
|---|---|
| 최대 확보 가능 총량 (보유 + 상한 기준 획득) | **최대(상한)** |
| 이번 로테이션 내 파견 가능 마리 수 | **최대(상한)** |
| 예상 코인 — 보너스 적용분 / 미적용분 분리 | **최대(상한)** |
| 슬롯 가동률, 이월 재고 | **최대(상한) 기준** |
| 보너스 마감까지 남은 시간 | **확정** (시각 계산이라 상한과 무관) |
| 마감 전 투입 가능 마리 수 | **최대(상한)** |
| 경계를 넘겨 완료되는 배치 | **조건부** — 다음 주 보너스를 알 수 없으므로 `X ~ 2X 코인` 범위로 표시 |
| 친구 소울 포함 시 추가분 | **추정** — 반드시 분리 표시 |
| 시즌 종료 전 소진 가능 여부 경고 | **경고** |

**성격이 세 갈래(최대 / 추정 / 확정)라는 걸 화면에서 구분할 것.** 시각 카운트다운만 확정이고, 마리 수·코인은 전부 상한 기준 최대치, 친구 소울은 추정이다.

### 5단계 (선택): 스톡 전략 비교

계산 대상 등급이 **이번 주 보너스가 아닐 때만** 노출:

```
A안 — 지금 다 파견:              최대 확보량 × 기본코인
B안 — 다음 보너스 주까지 쟁여두기: 최대 확보량 × 기본코인 × 2   (그 주에 이 등급이 보너스라는 전제)
차이 = 최대 확보량 × 기본코인
```

**다음 주 보너스를 미리 볼 수 없다는 게 확정됐으므로, 이 카드는 "만약 다음에 보너스가 오면" 조건부로만 표시한다.** 경험적 패턴으로 추측해서 "다음 주에 SSR이 올 확률" 같은 숫자를 만들어내면 안 된다.

여기에 **시즌 시한 경고**를 반드시 붙인다:

```
⚠️ 시즌 종료(2026-09-20)까지 26일 남았습니다.
   이 시즌 안에 SSR 보너스 주가 오지 않으면 쟁여둔 소울은 2배를 받을 수 없습니다.
   (전 시즌 소울은 파견은 되지만 보너스 대상이 아닙니다)
```

그리고 재고가 남은 보너스 주 소진 용량을 넘을 것 같으면:

```
⚠️ 현재 설정(슬롯 3개 / 하루 2회)으로는 보너스 주 7일 동안 최대 504마리까지만 소진할 수 있습니다.
   상한 기준 예상 재고 630마리 중 126마리는 넘치게 됩니다.
```

---

## 보너스 · 시즌 데이터를 어떻게 알 것인가

게임 연동이 없으므로, 이번 시즌 12종·주차별 보너스 3종·시즌 시작일을 어디선가 받아와야 한다.
초기 검토에 있던 "시즌 시작 시 4주 전체를 미리 입력" 안은 **게임이 미래 로테이션을 안 보여주는 게 확정되면서 폐기**됐다.

### 확정된 방식: 관리자 등록 UI(기본) + 선택형 폴백

**기본은 새우님이 `register.html`에서 직접 등록** — 기존에 캘린더 "일정 추가" 폼이 있는 그 관리자 페이지에, **"시즌 꽃 선택" 등록창을 새 섹션으로 추가한다. (확정 — v3 추가 결정)**
- 시즌 시작 시: 그 시즌의 짝꿍 꽃 12종(SSR 2 / SR 5 / R 5)을 이름으로 등록. 이때 dorazy.art에서 이름으로 찾은 `dorazyId`(F코드, → 썸네일 절 참고)도 같이 확정해서 저장.
- 매주: 그 12종 중 이번 주 보너스로 지정된 3종을 선택.
- 저장 방식은 캘린더 이벤트가 이미 `cozytools-worker`(Cloudflare Workers 백엔드)를 통해 저장되는 것과 **같은 패턴을 그대로 따른다** — 새 라우트/저장소 하나 추가하는 정도. 구체적인 스키마·API 형태는 기존 캘린더 저장 코드(`cozytools-worker/CLAUDE.md`)를 그대로 참고해서 Claude Code가 구현 시점에 정하면 됨(이 문서에서 미리 못박지 않음). **주의: 이름이 비슷한 `cozytools-backend`는 옛날에 쓰다 폐기된 Apps Script 버전이다 — 그쪽이 아니라 `cozytools-worker` 쪽에 붙일 것.**
- 이 등록창이 생기면 `seasons.json`을 **새우님이 git으로 직접 편집하는 게 주 경로가 아니게 된다** — 등록창 → 백엔드 저장 → `starsoul.html`이 그 데이터를 읽는 흐름이 기본이고, 정적 `seasons.json`은 (하이브리드로 갈 경우) 캐시/폴백 용도로만 남을 수 있음. 정적 파일이냐 백엔드 저장이냐는 구현 시 `cozytools-worker` 구조를 보고 정할 것 — 이 문서는 "등록창으로 입력한다"까지만 확정.

**폴백은 사용자 선택** — 새우님이 매주 일요일 자정에 실시간으로 못 적을 수 있으므로, **해당 주 데이터가 비어 있으면 사용자가 직접 고른다.**

> **폴백 UI는 자유 텍스트 입력이 아니라 선택형이어야 한다. (확정)**
> 이번 시즌 짝꿍 꽃 12종은 이미 등록돼 있으므로, **그 12개 중에서 3개를 체크박스로 고르는 UI**로 만든다. 오타·표기 흔들림이 원천적으로 안 생기고, 유저 입력이 3번 클릭으로 끝난다.
> 고른 값은 localStorage에 저장하되 **로테이션 경계를 넘으면 자동 폐기**한다.

### 데이터 구조 (히스토리 누적형)

역대 로테이션 기록 콘텐츠로 확장할 여지를 남기기 위해, **시즌을 배열로 계속 쌓는 구조**로 짠다. 지나간 시즌을 지우지 않는다.

```json
{
  "updatedAt": "2026-08-23",
  "timezone": "Asia/Seoul",
  "rotationBoundary": "SUN 00:00",
  "seasons": [
    {
      "id": "2026-08-23",
      "start": "2026-08-23",
      "weeks": 4,
      "souls": [
        { "name": "○○○", "grade": "SSR" },
        { "name": "△△△", "grade": "SR"  }
        /* … 12개 */
      ],
      "bonus": [
        { "week": 1, "start": "2026-08-23", "souls": ["○○○", "△△△", "□□□"] },
        { "week": 2, "start": "2026-08-30", "souls": null },
        { "week": 3, "start": "2026-09-06", "souls": null },
        { "week": 4, "start": "2026-09-13", "souls": null }
      ]
    }
  ]
}
```

- `souls: null` = 아직 미기록 → **선택형 폴백 발동**
- 나중에 "역대 로테이션 기록" 페이지는 이 파일 하나만 읽어서 만들 수 있다 (조합 패턴 통계도 여기서 나온다)
- `updatedAt`을 화면에 노출하고, 이번 주 데이터가 비어 있으면 안내 배너

### 시즌 시작일 출처 — 캘린더 연동

새우님이 이미 `cozytools` 이벤트 캘린더(`calendar.html` + `cozytools-worker` 백엔드)에 **"스타소울" 카테고리로 시즌 일정을 기록해오고 있고, 카테고리 필드가 존재하는 것도 확인됐다.**
다만 **그 필드로 실제 필터 조회가 안정적으로 되는지, 응답 스키마가 계산기가 쓸 만한 형태인지는 아직 미확인**이라 구현 시 실측이 필요하다.

**① 캘린더 API에서 시즌 시작일을 가져오는 안**

- 👍 **새우님이 이미 하는 작업을 그대로 재사용** — 캘린더에 적으면 계산기에도 자동 반영. 날짜를 두 곳에 적을 필요가 없다
- 👍 `calendar.html`이 같은 사이트에서 같은 워커를 이미 호출하고 있으므로 **CORS·호출 경로가 이미 뚫려 있고**, fetch 한 번 추가하는 정도라 **"정적 페이지" 원칙은 깨지지 않는다** (빌드 단계도, 서버 렌더도 없음)
- 👍 카테고리 필드가 있다는 건 확인됨 → 최소한 이벤트를 구분할 근거는 있다
- 👎 **캘린더가 줄 수 있는 건 날짜뿐이다.** 이벤트 스키마가 `이벤트명 / 시작일 / 종료일 / 설명`이라 **짝꿍 꽃 12종 목록과 주차별 보너스 3종이 들어갈 자리가 없다.** 설명 칸에 JSON을 욱여넣을 수는 있지만 캘린더 화면에 그 원문이 그대로 노출돼 지저분해진다
- 👎 **API 장애·네트워크 실패 시 계산기 첫 화면이 못 뜬다** → 폴백이 필수
- 👎 ⚠️ **카테고리로 필터 조회가 되는지, 응답 형태가 어떤지 미확인.** 이벤트명 표기가 흔들리면(`스타소울` vs `스타 소울`) 파싱이 조용히 깨진다 → 명명 규칙을 고정해야 함

**② 별도 `data/seasons.json`만 쓰는 안**

- 👍 12종 목록·주차별 보너스·히스토리가 한 파일에 자연스럽게 담긴다
- 👍 정적 파일이라 무장애, 오프라인에서도 뜬다
- 👎 새우님이 캘린더와 JSON 두 곳에 날짜를 적어야 한다

**③ 권장 — 하이브리드 (`seasons.json` 정본 + 캘린더 보조)**

```
1. data/seasons.json 을 읽는다 (정본: 12종, 주차별 보너스, 히스토리)
2. 캘린더 API 를 병렬로 호출해 "스타소울" 카테고리 이벤트를 조회한다 (가능하면)
   - 캘린더에 더 최신 시즌 시작일이 있으면 그걸 우선 사용 + 화면에 "캘린더 기준" 표기
   - 호출 실패/스키마 불일치/응답 없음 → 조용히 무시하고 seasons.json 값으로 진행
     (계산기는 어떤 경우에도 정상 동작해야 한다)
3. 이번 주 보너스가 어디에도 없으면 → 선택형 폴백 UI
```

이러면 새우님이 급할 때 **캘린더에만 날짜를 적어도 계산기의 시즌/주차 계산이 맞아 들어가고**, 12종·보너스는 여유 있을 때 JSON에 채우면 된다. 캘린더가 죽어도 계산기는 안 죽는다.

> **구현 순서 권장**: ②번(`seasons.json`만)으로 먼저 완성해서 동작을 확보하고, 그다음에 캘린더 조회를 **선택적 보강**으로 얹는다. 실측해보고 카테고리 필터가 쓸 만하지 않으면 **①을 포기하고 ②로 확정해도 전혀 문제없다** — 그게 더 단순하고, 새우님 부담도 28일에 한 번이라 크지 않다.

---

## UI 구성

전체 1페이지, 위→아래 흐름. **모바일 390px 확인 필수** (`CLAUDE.md` 원칙과 동일).

### ① 현재 보너스 카드 (최상단, 입력 없이 바로 보임)

```
┌────────────────────────────────────────┐
│  이번 주 보너스          시즌 1주차 / 4  │
│                                         │
│  [🖼️SSR] 꽃이름  [🖼️SR] 꽃이름  [🖼️R] 꽃이름 │
│                                         │
│  다음 로테이션까지     0일 23:14:07       │
│  시즌 종료까지        27일 23:14:07       │
│  (일요일 00:00 KST 기준 · 획득 상한도 같이 초기화) │
│                                         │
│  데이터 최종 갱신 2026-08-23 · 직접 선택  │
└────────────────────────────────────────┘
```

- 카운트다운은 `setInterval` 1초. 경계를 넘기면 자동 재계산/재렌더.
- "직접 선택" → **12종 중 3개 체크박스 폴백 UI** (접힘).
- 이번 주 데이터가 비어 있으면 카드 위에 안내 배너 + 폴백 UI 자동 펼침.
- **꽃 썸네일은 dorazy.art 핫링크. (확정)** `<img src="https://dorazy.art/images/t/F0XXX.webp">` 형태로 직접 참조 — 다운로드/자체 커밋 안 함. dorazy.art가 일시적으로 안 뜨면 썸네일만 깨지고 텍스트(이름·카운트다운·계산 결과)는 정상 동작해야 하므로 `<img onerror>`로 대체 텍스트/아이콘 처리 필수. `seasons.json`의 소울 항목에 `"dorazyId": "F0123"` 필드를 추가해서 저장해 둘 것 — 이 ID는 ② 등록창(아래)에서 새우님이 시즌 꽃을 고를 때 같이 확정된다.

### ② 보너스 마감 카운트다운 (이 도구에서 제일 실용적인 부분)

계산 대상 등급이 **이번 주 보너스일 때** 노출:

```
⏳ SSR 보너스 마감까지 21:14:07  (토 00:00 KST)
   12마리 꽉 채운 배치 기준 마감이에요 — 이 시각 이후에 12마리를 걸면 완료가 다음 주로 넘어가 2배를 못 받습니다.
   더 적게 보내면 이후에도 아슬아슬하게 맞출 수 있습니다(상세 타임라인에서 확인).
   지금부터 마감 전까지 최대 N마리를 보너스로 태울 수 있습니다. (N은 시뮬레이션이 계산)
```

마감이 지났거나 대상 등급이 비보너스면 트릭 안내로 전환:

```
⏰ 지금 SSR을 걸면 완료가 다음 로테이션으로 넘어갑니다.
   다음 주에도 SSR이 보너스라면 2배가 적용됩니다 (아니어도 손해는 없습니다).
   ※ 게임이 다음 주 보너스를 미리 보여주지 않아 확인은 불가능합니다.
```

### ③ 입력부

- 등급 탭: `SSR | SR | R`
- 보유 수량 (숫자 입력, 큰 글씨)
- 획득 경로 토글: `무료 상한 12/일` ↔ `패스권 상한 30/일`
- **슬롯 개수 선택: `2 | 3 | 4 | 5` (기본 5)** — 옆에 "기본 2개, 확장 시 최대 5개" 안내
- **("하루 파견 갱신 횟수 `k`" 입력은 뺐다 — 실제 구현은 "슬롯이 비면 즉시 재고가 있는 만큼 바로 채운다"는 이론 최대치 가정으로 시뮬레이션하므로 이 입력이 필요 없다. → 구현 메모)**
- **오늘 남은 획득 가능 개수** (0~상한, 기본 0) — 문구: **"오늘 획득 상한이 얼마나 남았나요?"** (※ "지급받았나"가 아니다)
- 친구 소울 0~4 슬라이더 — 문구: **"목표 등급을 심은 친구에게서 하루 몇 개나 확실히 받을 수 있나요?"**, 옆에 "추정치에만 반영됨" 표기
- 전제 안내문: **"짝꿍 꽃을 이 등급 한 종류로 집중해서 심는 중"** + **"하루 12/30개는 획득 상한 — 결과는 이론 최대치"**
- 고급 설정(접힘): 활동 시간대, **하루 상한 달성률(%) — 기본 100**

### ④ 결과부

**[최대 확보 가능 — 상한 기준 이론치]** — 실선 카드, 진한 색. **제목에 "최대"를 반드시 넣을 것**

```
이번 로테이션 종료까지 (23시간 14분 남음)

  최대 확보 가능    120마리   (보유 120 + 상한 기준 획득 0)
  최대 파견 가능    120마리   (슬롯 여유 있음, 가동률 22%)
  이월 재고           0마리

  예상 코인          960 코인  (이론 최대치)
    └ 보너스 적용     960   (SSR 보너스 주간, 마감 전 투입분)
    └ 미적용            0
    └ 경계 넘김         0   (다음 주 보너스 여부에 따라 0 ~ 0)

  ※ 하루 12/30개는 획득 상한입니다. 매일 상한을 다 채웠을 때의 값이라
    실제로는 이보다 적을 수 있습니다.
```

**[추정 — 친구 소울]** — 점선 테두리, 회색 배경, 명확히 분리

```
친구 소울 4개/일 기준 최대 +4마리 → +32코인
※ 친구가 그 등급을 심었는지에 따라 달라지는 참고 수치입니다.
```

**[경고]** — 노란 배경. 시즌 시한·소진 용량 초과 경고 (위 5단계 참고)

**[스톡 전략 비교]** — 대상 등급이 이번 주 보너스가 아닐 때만

**[상세 타임라인]** — 접힘. 각 배치의 시작/완료 시각, 마리 수, 적용 배율. **"완료 시점 판정"이 왜 그렇게 나왔는지 사용자가 눈으로 검증**할 수 있게 하는 용도.

---

## 구현 메모

- **파일**: `cozytools/starsoul.html` 단일 파일 + `cozytools/data/seasons.json`. 인덱스 페이지에 카드/링크 추가. 스프레드시트 연동 **없음**. 캘린더 워커 API는 (하이브리드 안을 택할 경우) 읽기 호출 1개만 추가.
- **시간대**: KST는 서머타임이 없으므로 **UTC+9 고정 오프셋**으로 계산하는 게 가장 안전하다. `new Date()`의 로컬 타임존에 의존하는 코드를 쓰지 말 것 — 해외 접속자가 있으면 경계가 통째로 틀어진다. 헬퍼 세 개(`toKST(date)`, `nextRotationBoundary(now)`, `bonusDeadline(now, grade)`)를 만들어 놓고 전부 그걸 거치게 할 것.
- **자정 처리는 하나로 묶는다.** 획득 상한 초기화와 로테이션 변경이 **같은 자정에 동시에** 일어나므로, 순서를 따질 필요도 실측할 필요도 없다. 자정 경계 핸들러 하나가 ① 캡 리셋 ② 보너스 세트 교체를 같이 처리하면 된다.
- **"지급"이라는 표현을 코드·주석·UI 어디에도 쓰지 말 것.** 변수명도 `dailyGrant` 같은 게 아니라 `dailyCap` / `remainingCapToday` 로. 모델을 잘못 이해한 채로 나중에 손대는 걸 막는다.
- **결과 라벨은 전부 "최대/이론치"로.** `확정 획득`, `확보 확정` 같은 문구가 들어가면 안 된다. 시각 카운트다운만 확정이다.
- **오프바이원 주의**: 완료 시각이 경계와 **정확히 같을 때**를 어느 쪽으로 볼지 코드에 명시할 것. 문서 기준은 `완료시각 >= 경계시각 → 다음 주`.
- **슬롯 개수는 절대 상수로 박지 말 것.** `SLOTS = 5` 같은 코드가 나오면 안 된다. 상단 상수는 `SLOT_MAX = 5`, `SLOT_MIN = 2`, `BATCH_MAX = 12`.
- **배치 크기 1~12 자유**: "12마리 단위" 가정을 쓰지 말 것. 마지막 배치의 나머지 처리 특수 로직이 필요 없다.
- **입력 기억**: 보유 수량·패스권 여부·슬롯 개수·달성률을 localStorage에 저장. 개인정보가 아니고 브라우저 안에서만 산다. **보너스 폴백 선택값과 "오늘 남은 획득 가능 개수"는 자정 경계를 넘으면 자동 폐기** — 오래된 값이 남아 틀린 정보를 보여주는 게 최악이다.
- **입력 검증**: 음수·비정수·빈 값·비현실적으로 큰 값(예: 99999) 방어. **0 입력도 정상 동작해야 한다** ("지금 0개여도 로테이션 끝까지 최대 N마리 확보 가능") — 신규 유저에게 오히려 이게 유용한 사용법이다.
- **"추정" 분리**: 친구 소울 결과가 본 계산 수치와 섞이면 안 된다. 카드를 분리하고 합계에 더하지 말 것. "합쳐 보기" 토글은 가능하되 기본 꺼짐, 켰을 때 숫자 색을 바꿔 추정임을 계속 드러낼 것.
- **상수 분리**: 등급별 코인/소요시간, 슬롯 최대/최소, 배치 상한, 일일 획득 상한, 보너스 배율(2)을 파일 상단 상수 객체로 모아둘 것. 밸런스 패치가 오면 거기만 고치면 되게.
- **v2 메모 — 가속 계산**: 가속은 5분당 1다이아, 즉시 완료만 가능(부분 가속 없음)으로 확정됐다. 즉 `가속 비용 = ceil(남은 시간 / 5분) 다이아`. 보너스 마감 직전에 가속으로 슬롯을 비워 한 번 더 태우는 게 이득인지가 계산 가능한 문제다 — **v1에는 넣지 말고, v2 기능 후보로만 둘 것.** (새우님: "염두에 두고 있지만 일단 보류")
- **모바일**: 390px에서 결과 카드의 숫자가 줄바꿈으로 깨지기 쉽다. 등급 탭 / 슬롯 선택 / 결과 카드 위주로 실기기 확인.
- **꽃 썸네일은 dorazy.art 핫링크 (확정, v3 추가).** 자체 다운로드/커밋 안 함 — `<img src="https://dorazy.art/images/t/{dorazyId}.webp" onerror="...">`. `onerror`로 깨진 이미지 아이콘 대신 등급 배지 정도로 대체할 것 — 썸네일이 안 뜨는 게 계산기 신뢰도에 영향 주면 안 된다.
- **`register.html`에 "시즌 꽃 선택" 등록창 추가 (확정, v3 추가).** 기존 "일정 추가" 폼 옆에 새 섹션. 시즌 시작 시 12종(이름 + dorazy.art `dorazyId`) 등록, 매주 그중 3종을 보너스로 선택. 저장은 캘린더 이벤트와 같은 **`cozytools-worker`**(Cloudflare Workers) 경로를 따를 것 — 새 엔드포인트/저장소 스키마는 기존 캘린더 저장 코드 패턴을 그대로 참고해서 구현 시점에 정할 것. `starsoul.html`은 이 데이터를 읽어서 ①번 카드를 채운다. (옛날 이름이 비슷한 `cozytools-backend`는 폐기된 Apps Script 버전이니 헷갈리지 말 것.)
- **🐛 버그 수정 이력 (2026-08-23)**: `GRADE_INFO[grade].hours`를 배치 완료시간에 그대로 쓰면(마리 수를 안 곱하면) SSR 12마리가 24시간이 아니라 2시간 만에 끝나는 버그가 난다 — 실제로 새우님이 발견해서 고침. 영향받은 4곳: ① `bonusDeadline()` (마리당시간 × 12를 빼야 함), ② 시뮬레이션의 배치 완료시각 계산(마리당시간 × 이번 배치 마리 수), ③ `maxSlotCapacity()`의 사이클 수 계산(마리당시간 × 12로 나눠야 함), ④ 보너스 주 소진 용량 경고의 `weeklyCycles` 계산(위와 동일). **앞으로 이 상수를 건드리거나 비슷한 계산을 새로 추가할 때, "소요시간은 마리당"이라는 전제를 반드시 지킬 것** — 위 "파견 시스템" 절의 경고 박스 참고.

---

## 아직 정할 것 / 열린 질문

v1·v2에서 열려 있던 항목 중 **로테이션 경계, 일일 상한 초기화 시각, 획득 등급 결정 방식, 미래 로테이션 예고, 전 시즌 소울, 파견 개수, 가속/취소, 슬롯 개수, 보너스 배율, 데이터 관리 방식, 12/30의 성격(상한), 자정 이벤트 순서, 혼합 심기 지원 여부**는 전부 답이 나와 본문에 반영했고 여기서 뺐다. 남은 건 다음뿐이다.

1. **캘린더 워커 API 연동의 실효성.** ✅ "스타소울" **카테고리 필드가 존재하는 것은 확인됨.** ⚠️ 다만 ① 그 카테고리로 **필터 조회가 안정적으로 되는지**, ② **응답 스키마가 계산기가 쓸 만한 형태인지**는 미확인이라 **구현 시 실측이 필요하다.** 그리고 새우님이 "스타소울" 이벤트를 적을 때의 **명명 규칙을 고정**해야 파싱이 안전하다(예: 이벤트명 `스타소울 시즌 N` + 시작일 = 시즌 시작일). **실효성이 없다고 판단되면 `seasons.json`만 쓰는 안으로 확정해도 문제없다.**
2. **슬롯 확장 조건/비용.** 계산에는 필요 없고, 슬롯 선택 UI 옆 안내 문구용이다. ("확장 비용이 비싸지 않아 대부분 5개까지 확장" 정도로만 적어도 충분)
3. **시즌/보너스 데이터 갱신 운영.** `register.html`의 "시즌 꽃 선택" 등록창으로 입력하는 걸로 확정됐지만(→ 데이터 절 참고), 그래도 새우님이 매주 일요일 자정에 실시간으로 못 누를 수 있다는 전제는 그대로다. **선택형 폴백 UX가 실제로 자연스러운지**를 배포 후 한 주 돌려보고 확인할 것. 폴백이 불편하면 "지난주 값 유지 + 경고" 같은 완충을 추가하는 것도 방법.
4. **v2 — 가속(다이아) 계산.** 규칙은 확정됐으니 언제든 붙일 수 있지만 **현재 보류.** 보너스 마감 직전 가속이 이득인 조건을 계산해주는 기능.
5. **v2 — 역대 로테이션 기록.** `seasons.json`이 히스토리 누적형이므로 그 파일만 읽어서 만들 수 있다. 조합 패턴(`SSR+SR+R` / `SR+R+R`) 통계도 여기서 나온다. **v1 필수 아님, 구조만 열어둘 것.**
6. **2단계 — 상점 / 코인 소비 계산.** 상점 시즌이 ~150일로 파견 시즌(28일)과 주기가 완전히 달라서 같은 화면에 억지로 넣으면 오히려 혼란스럽다. 이번 계산기를 배포하고 반응을 본 뒤에 별도로 논의.

---

## 시작할 때 참고

- **`cozytools/CLAUDE.md`를 먼저 읽을 것** (사이트 구조, 배포 방식, 페이지 추가 규칙). 이건 `guild-flower`가 아니라 `cozytools` 프로젝트다 — Apps Script·스프레드시트 없음.
- 기존 `calc.html`의 **입력 → 즉시 계산 → 카드형 결과** 패턴과 `calendar.html`의 **날짜 계산 헬퍼 및 워커 API 호출 코드**를 그대로 가져다 쓰면 스타일도 맞고 작업량도 준다. 특히 캘린더 연동을 얹을 때 `calendar.html`의 fetch 코드가 거의 그대로 재사용된다.
- **①번 "현재 보너스 카드" + ②번 "마감 카운트다운"부터 만들 것을 추천.** 입력 없이 열자마자 값이 보이는 화면이라 새우님이 바로 검증할 수 있고, 커뮤니티 공유 시 훅으로도 가장 강하다. 계산 로직은 그다음.
- **첫 시즌 데이터는 2026-08-23 시작으로 이미 알고 있다.** `seasons.json`에 이 날짜와 12종 목록을 채워 넣고 시작하면 주차 계산을 바로 검증할 수 있다.
- 시뮬레이션(3단계)을 구현할 때 **상세 타임라인 출력을 먼저 만들어 눈으로 검증**한 뒤 요약 숫자를 뽑는 순서가 안전하다. 보너스 완료 시점 판정과 자정 캡 리셋이 오프바이원이 나기 쉬운 지점이다.
- **결과 문구를 쓸 때 "최대/이론치"라는 성격을 계속 지킬 것.** 이 도구가 신뢰를 잃는 가장 빠른 길은 "확정 3,000코인"이라고 써놓고 유저가 절반밖에 못 얻는 것이다.
- 배포 후 `backlog.md`(그리고 PC의 `TODO.md`)의 cozytools 절에 완료 기록을 남길 것.
