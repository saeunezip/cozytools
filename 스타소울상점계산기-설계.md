# 스타소울 상점 계산기 — 설계 확정 (v1, 2026-08-24)

Claude Code(PC 로컬)에서 `cozytools/shop.html`을 만들 때 참고할 설계 문서.
대상 프로젝트는 **`cozytools`** (`C:\Users\kyung\Documents\cozytools`, 리포 `saeunezip/cozytools`). GitHub Pages(`main`, `cozytools.kro.kr`) 배포, 빌드 없는 정적 HTML/JS.

`스타소울파견계산기-설계.md`의 열린 질문 6번("상점 / 코인 소비 계산은 별도 논의")에 대한 답이 이 문서다.

> **✅ 2026-08-24 새우님 확인 반영 (구현 완료):**
> 1. **소울당 코인(7/4)은 편집 불가로 고정.** 게임 오픈 이후 4개월간 "SSR 보너스 2주 + SR 보너스 2주" 패턴이 유지된 확정치라 고급 설정 없앰 — 화면에는 근거 설명만 남김.
> 2. **시즌 기본값 1x** 확정(원래 기본값이었음).
> 3. **"이벤트" = 28일 시즌 1:1 확정.** 실제 게임 표기는 "스타소울-사자자리" / "스타소울-처녀자리" 식. 실제 별자리·날짜가 정확히 안 맞을 수 있지만 28일 주기 자체는 확정.
> 4. **유예 기간은 15일이 아니라 14일.** (11/8 00:00 카운트다운 종료 + 14일 = 11/22 00:00, 새우님 확인) `GRACE_DAYS = 14`로 정정.
> 5. **마감 후 남는 코인은 소멸** — 확정.
> 6. **상품 목록은 계속 하드코딩.**
> 7. **보유 소울 재고 반영은 계속 v1 범위 밖.**
> 8. **시즌 이름은 앵커 추정이 아니라 `register.html`에서 실제 등록.** `/seasons`에 `name` 필드 추가(`addSeason`에 포함, `setSeasonName`으로 나중에 수정 가능). `shop.html`이 이걸 가져다 써서 없으면 **"(예정)"** 으로 표시 — 앵커 기반 별자리 순서 추정 로직은 삭제.
> 9. **친구 소울 추가.** 하루 4개 × 소울당 4코인(등급을 모르니 안전한 평균)로 계산해서 체크박스로 켜고, 결과에 별도 줄로 표시.
>
> 아래 본문의 "3단계: 시즌 열거 + 이름"(앵커 기반 `ZODIAC` 배열 추정)과 열린 질문 1·2·4·5·8·9는 위 확정 내용으로 대체됐다 — 본문은 날짜 분배 로직(0~7단계 중 이름 부분 제외)과 UI 레이아웃 참고용으로만 남겨둔다.

> **⚠️ 기존 `shop.html`(2026-08-24 1차본)의 계산 모델은 전부 폐기한다.**
> 1차본은 `하루 상한 × 남은 일수 × 등급 코인`으로 계산하고 결과를 **`최소 ~ 최대` 범위**(전부 1배 / 전부 2배)로 보여줬다. 이게 폐기되는 이유:
> - **범위가 너무 넓어서 쓸모가 없다.** 2배 차이 나는 범위를 던져주면 "살 수 있는지"에 답이 안 된다.
> - **보너스 적중률은 게임이 정하는 게 아니라 유저가 정한다.** 새우님처럼 보너스 주에 맞춰 등급을 갈아타면 2x, 귀찮아서 안 하면 1x — 이건 예측 대상이 아니라 **입력 대상**이다.
> - **상점 마감 시점이 틀렸다.** 게임 카운트다운이 끝이 아니라 **15일 유예**가 더 있다.
> - **28일 시즌 경계를 무시했다.** `days / 28`로 소수점 시즌 수만 표시하고 계산엔 안 썼다.
>
> UI 스타일/상품 목록/localStorage 패턴은 1차본에서 그대로 가져와도 되지만, **`recalc()` 이하 계산부와 입력 필드 구성은 전면 교체**다.

---

## 목적 / 배경

`starsoul.html`(파견 계산기)이 **"지금 이 로테이션에서 어떻게 굴려야 코인을 제일 많이 먹나"**를 초 단위 시뮬레이션으로 푸는 도구라면, 이 상점 계산기는 정반대다:

> **"상점 문 닫기 전까지 내가 코인을 얼마나 모을 수 있고, 그걸로 저거 살 수 있냐?"**

이 질문에 필요한 건 정밀도가 아니라 **긴 기간에 걸친 어림값**이다. 상점 마감까지 보통 2~5개월이 남고, 그 사이에 파견 시즌이 3~6번 바뀐다. 그 기간을 슬롯 단위로 시뮬레이션하는 건 정확해 보일 뿐 실제로는 의미가 없다 — 유저가 90일 동안 매일 상한을 꽉 채울 리도 없고, 미래 보너스 로테이션은 애초에 알 수도 없다.

그래서 이 도구는 **"하루 평균 몇 코인"이라는 정액 요율**을 쓴다. 그 요율은 시뮬레이션에서 뽑아내는 게 아니라 **새우님의 실제 플레이 관측치를 상수로 박은 것**이고, **시즌마다 그 요율을 유저가 직접 고른다.**

### `starsoul.html`과의 관계 (혼동 방지)

| | `starsoul.html` (파견) | `shop.html` (상점) |
|---|---|---|
| 시간 축 | 이번 주 로테이션 (~7일) | 상점 마감까지 (~60~150일) |
| 계산 방식 | 이벤트 기반 슬롯 시뮬레이션 | **하루 정액 요율 × 일수** |
| 보너스 | `/seasons`에서 실시간 로드 | **안 씀** (요율에 이미 녹아 있음) |
| 쟁여두기 최적화 | 핵심 기능 | **가정하지 않음** (즉시 파견 전제) |
| 슬롯 개수 입력 | 있음 | 없음 |
| 등급 선택 | 있음 | 없음 (요율이 등급 선택을 이미 포함) |

**두 도구는 계산 모델을 공유하지 않는다.** `shop.html`에서 `GRADE_INFO`·슬롯 시뮬레이션·`/seasons` 보너스 조회를 재사용하려 들지 말 것. 공유하는 건 **KST 날짜 헬퍼뿐**이다 (→ 구현 메모).

### 이번 v1의 범위 (여기서 벗어나지 말 것)

- ✅ 상점 카운트다운 입력 → **15일 유예를 더한 진짜 마감 시각** 산출 + 라이브 카운트다운
- ✅ 마감까지 걸치는 **별자리 시즌 자동 열거**, 시즌마다 **2x / 1x 직접 지정**
- ✅ 정액 요율 × 시즌별 일수 → **예상 총 코인** (보유 코인 포함)
- ✅ 상품별 구매 가능 여부 + 여러 개 골랐을 때 합계 판정
- ❌ **범위(min~max) 표시 금지.** 유저가 시즌별로 2x/1x를 골랐으면 답은 **하나의 숫자**다.
- ❌ **슬롯/등급/배치 시뮬레이션 금지.** 그건 `starsoul.html`이 하는 일이다.
- ❌ **미래 보너스 로테이션 조회 금지.** 게임이 안 보여준다(파견 설계 문서 확정 사항). 시즌별 2x/1x는 **유저의 계획**이지 예측이 아니다.
- ❌ **현재 보유 소울 재고를 코인으로 환산해 더하는 것은 v1에서 뺀다.** (→ 열린 질문 7)

---

## 확정된 게임 규칙

새우님 확인 내용 반영. **⚠️ 표시가 붙은 곳만 미확정.**

### 상점 마감 · 15일 유예

- 상점은 게임 화면에 **자체 카운트다운**을 띄운다. 표기 형식은 **`카운트다운: 75일 22시간`** — 일/시간 두 단위.
- **이 카운트다운은 파견 시즌(28일)과 완전히 별개 주기다. (확정)** 상점은 100일 단위쯤으로 훨씬 길고, 시즌 경계와 정렬돼 있지 않다.
- **카운트다운이 0이 돼도 그 상점은 바로 안 닫힌다. (확정)**
  카운트다운 종료 후 **15일 동안 현재 상점과 다음 상점이 동시에 열려 있고**, 그동안 현재 상점 상품을 계속 살 수 있다.
  ```
  진짜 마감 = 게임 카운트다운 종료 + 15일
  ```
  **→ 계산기는 항상 "진짜 마감" 기준으로 계산한다.** 게임 표기 숫자를 그대로 쓰면 15일치(최대 3,150코인)를 손해 보게 만든다.
- ⚠️ **15일이 정확한 값인지, 달력일 기준인지는 새우님 관측치다.** 상수 하나(`GRACE_DAYS = 15`)로 빼두고 화면에도 "유예 15일 포함"이라고 명시해서, 틀렸을 때 바로 고칠 수 있게 할 것. (→ 열린 질문 4)

### 코인 획득 — 하루 정액 요율 모델 (확정)

**이 계산기의 핵심이자, 1차본과 가장 크게 달라지는 부분.**

새우님의 실제 플레이 기준 관측치:

| 플레이 방식 | 소울당 평균 코인 | 하루 획득 상한 | **하루 코인** |
|---|---|---|---|
| **2x 운영** (보너스 주에 맞춰 등급 갈아타기) | **7** | 30 (패스권) | **210** |
| **1x 운영** (보너스 신경 안 씀) | **4** | 30 (패스권) | **120** |

**요율이 저 숫자인 이유 (반드시 코드 주석에 남길 것):**

- **2x = 소울당 평균 7코인.** 4주 시즌 동안 보통 **SSR 보너스 2주 + SR 보너스 2주**가 걸린다. SSR 보너스는 마리당 8코인, SR 보너스는 마리당 6코인 → **(8+8+6+6)/4 = 7**.
- **1x = 소울당 4코인.** 보너스를 안 노리면 등급 선택에 아무 이유가 없으므로 **무조건 소울당 코인이 제일 높은 SSR만 재배**한다. SSR 기본 코인 = 4.
  (파견 설계 문서 "핵심 계산 결과 1번" — 슬롯 시간당 효율은 등급이 전부 동일하므로, 보너스가 없으면 소울당 코인이 최고인 SSR이 무조건 정답이다. 이 모델은 그 결론과 정합한다.)
- **× 30 = 유료 패스권의 하루 획득 상한.** 무료 유저는 12다.

**→ 요율은 두 축의 곱으로 분해해서 구현한다. 210/120을 매직 넘버로 박지 말 것:**

```
하루 코인 = 소울당코인(운영방식) × 하루상한(획득경로)

              소울당코인    무료(12)   패스권(30)
  2x 운영         7           84         210
  1x 운영         4           48         120
```

이렇게 두면 **무료 유저도 그냥 지원된다** — 획득 경로 토글 하나로 (84, 48) 쌍으로 바뀐다. 210/120만 상수로 박으면 무료 유저에게 2.5배 뻥튀기된 답을 주게 된다.

**이 모델이 전제하는 것 (화면에 명시할 것):**

- **즉시 파견 전제.** 쟁여뒀다가 보너스 주에 몰아 태우는 최적화(`starsoul.html`의 스톡 전략)는 **가정하지 않는다.** 소울이 생기는 대로 바로 파견하는 평범한 플레이 기준이다. 스톡 전략을 잘 쓰면 이보다 더 나온다.
- **친구 소울 미포함. (확정)** 새우님 관측치가 친구 도움 없이 나온 값이다. 친구 소울까지 챙기면 이보다 더 나온다.
- **매일 상한을 꽉 채운다는 전제.** 파견 설계 문서와 같은 성격 — **이건 "이론 최대치"다.** 깜빡하는 날이 있으면 그만큼 준다.
- ⚠️ **슬롯 2개짜리 유저에게는 210이 과대평가일 수 있다.** 파견 설계 문서 "핵심 계산 결과 3번" 기준, SSR을 슬롯 2개로 굴리면 주간 처리량 168마리 < 획득량 210마리로 슬롯이 병목이다. 슬롯 3개 이상이면 여유가 있다. **v1은 슬롯 입력을 받지 않되, 안내 문구로만 알린다.**

### 별자리 시즌 — 이름과 주기 (확정)

- 파견 시즌 = **28일**, 항상 **일요일 00:00 KST 시작**. (파견 설계 문서 확정 사항)
- 시즌 이름은 **서양 12별자리를 표준 순서대로** 붙인다:
  ```
  양자리 · 황소자리 · 쌍둥이자리 · 게자리 · 사자자리 · 처녀자리 ·
  천칭자리 · 전갈자리 · 사수자리 · 염소자리 · 물병자리 · 물고기자리
  ```
  12개를 다 돌면 **336일**이 걸린다.
- **확인된 실제 데이터 (앵커):**

  | 시즌 시작일 | 요일 | 이름 |
  |---|---|---|
  | 2026-07-26 | 일 | **사자자리** ✅ 확인됨 |
  | **2026-08-23** | 일 | **처녀자리** ✅ 확인됨 (현재 시즌, `/seasons`의 `id: "2026-08-23"`와 일치) |
  | 2026-09-20 | 일 | 천칭자리 (표준 순서로 유도) |
  | 2026-10-18 | 일 | 전갈자리 |
  | 2026-11-15 | 일 | 사수자리 |
  | 2026-12-13 | 일 | 염소자리 |

  두 실측치가 정확히 28일 간격이고 표준 별자리 순서와 맞아떨어진다 → **`2026-08-23 = 처녀자리`를 앵커로 삼고 앞뒤로 28일씩 외삽한다.**
- ⚠️ **미래 이름은 유도값이지 확인값이 아니다.** 첫 경계(2026-09-20)가 지나면 게임에서 "천칭자리"가 맞는지 새우님이 눈으로 확인해줄 것. 어긋나면 앵커 상수만 고치면 된다.

---

## 계산 로직

### 0단계: 상수

```js
const ANCHOR_START = "2026-08-23";   // 처녀자리 시작 (일요일, /seasons id와 동일)
const ANCHOR_INDEX = 5;              // ZODIAC 배열에서 처녀자리의 인덱스
const ZODIAC = ["양자리","황소자리","쌍둥이자리","게자리","사자자리","처녀자리",
                "천칭자리","전갈자리","사수자리","염소자리","물병자리","물고기자리"];
const SEASON_DAYS = 28;
const GRACE_DAYS  = 15;              // 카운트다운 종료 후 유예 (⚠️ 관측치)
const COIN_PER_SOUL = { x2: 7, x1: 4 };   // 2x 운영 평균 / 1x 운영(SSR 고정)
const DAILY_CAP     = { free: 12, pass: 30 };
```

### 1단계: 진짜 마감 시각

유저는 게임 화면의 카운트다운을 **일 / 시간 두 칸**에 그대로 옮겨 적는다 (`75`, `22`).

```
입력시각 t0 = 입력을 받은 순간 (new Date())
카운트다운 종료 = t0 + 일×86400000 + 시간×3600000
진짜 마감 DEADLINE = 카운트다운 종료 + GRACE_DAYS × 86400000
```

> **⚠️ 절대 시각으로 굳혀서 저장할 것.** "남은 일수"를 상태로 들고 있으면 페이지를 켜둔 채 하루가 지나거나, 내일 다시 방문했을 때 값이 조용히 틀려진다.
> `localStorage`에는 `shop_deadlineMs`(숫자 하나)만 저장하고, 그 이후 모든 계산과 카운트다운은 이 절대 시각에서 파생시킨다. 입력칸 두 개는 "마감 시각을 만들어내는 도구"일 뿐 상태가 아니다.
> 재방문 시엔 저장된 마감 시각과 남은 시간을 보여주고, **"카운트다운 다시 입력"** 버튼으로만 갱신하게 한다.

> **게임 표기는 내림값이다.** "75일 22시간"이면 실제 남은 시간은 75일 22시간 ~ 75일 23시간 사이다. 내림값을 그대로 쓰면 최대 1시간(≈9코인) 보수적으로 잡힌다 — **의도된 방향이므로 보정하지 말 것.**

화면에는 유도 과정을 그대로 보여줘서 유저가 검증할 수 있게 한다:

```
게임 카운트다운 종료   11/8(일) 08:00
유예 기간             + 15일
─────────────────────────────
진짜 마감             11/23(월) 08:00 KST   (90일 22:00:00 남음)
```

### 2단계: 일 번호(dayNum) 체계

시즌 경계도 자정, 획득 상한 초기화도 자정이므로 **모든 기간 계산을 "KST 달력 날짜 번호" 정수로** 한다. 소수점 일수를 쓰지 말 것 — 오차와 오프바이원의 근원이다.

```
dayNum(t) = floor( (그 시각이 속한 KST 날짜의 00:00 − ANCHOR_START의 00:00) / 86400000 )
```

`ANCHOR_START` 당일이 `0`. 시즌 `k`는 **`[28k, 28k+28)`** 구간(half-open)을 차지한다.

**계산에 포함할 날짜 범위:**

```
firstDay = dayNum(now) + (오늘분포함 ? 0 : 1)
lastDay  = dayNum(DEADLINE) − 1
총 일수 D = max(0, lastDay − firstDay + 1)
```

- **오늘(부분일)은 기본 제외.** 유저가 오늘 이미 상한을 다 썼는지 계산기는 알 수 없고, 이 도구의 실패 모드는 "살 수 있다고 해놓고 못 사는 것"이다. **`오늘 하루치 아직 남았어요` 체크박스**(기본 꺼짐)로 켜면 `+1일`.
- **마지막 부분일은 항상 제외.** `lastDay = dayNum(DEADLINE) − 1` 한 줄로 두 경우가 다 처리된다:
  - 마감이 그날 08:00 → 그날은 온전한 하루가 아니므로 제외
  - 마감이 정확히 00:00 → 그날은 애초에 마감 이후이므로 제외

  게임 규칙상으로도 맞다 — 마지막 날 파견을 걸어도 **완료가 마감 뒤로 넘어가면 그 코인은 못 쓴다.**

### 3단계: 시즌 열거 + 이름

```
kFirst = floor(firstDay / 28)
kLast  = floor(lastDay  / 28)

for k = kFirst .. kLast:
    시즌시작 dayNum = 28k
    이름 = ZODIAC[ ((ANCHOR_INDEX + k) % 12 + 12) % 12 ]     // k가 음수여도 안전하게
    시즌시작 날짜 = ANCHOR_START + 28k일
```

`% 12`가 음수를 낼 수 있으므로 **`((x % 12) + 12) % 12`** 형태를 쓸 것 (앵커 이전 시즌을 표시할 일은 없지만, 유저가 마감을 이상하게 입력했을 때 `undefined`가 뜨는 걸 막는다).

### 4단계: 시즌별 일수 배분

```
days[k] = max(0, min(lastDay, 28k + 27) − max(firstDay, 28k) + 1)
```

- **현재 시즌(`k = kFirst`)**: 이미 지나간 날은 자동으로 빠진다. 화면에 **"이미 N일 지남 · 남은 M일만 반영"**이라고 표기해서 왜 28일이 아닌지 유저가 납득하게 할 것.
- **마지막 시즌(`k = kLast`)**: 마감이 시즌 중간에 떨어지면 그만큼만 잡힌다. **"마감이 시즌 중간 · M일만 반영"** 표기.
- **검산**: `Σ days[k] === D`. 이게 안 맞으면 오프바이원이다. **개발 중 `console.assert`로 걸어둘 것.**

### 5단계: 코인 합계

유저는 **시즌마다 `2x` / `1x`를 직접 고른다** (기본값 → 열린 질문 2).

```
소울당코인[k] = (그 시즌 선택 === "2x") ? 7 : 4
하루코인[k]   = 소울당코인[k] × DAILY_CAP[획득경로]
시즌코인[k]   = 하루코인[k] × days[k]

예상 획득 코인 = Σ 시즌코인[k]
사용 가능 코인 = 현재 보유 코인 + 예상 획득 코인
```

### 워크드 예제 (구현 후 이 값으로 검증할 것)

**입력**: 지금 = `2026-08-24(월) 10:00 KST` / 카운트다운 `75일 22시간` / 패스권 / 오늘분 미포함 / 보유 코인 0

```
dayNum(now)      = 1
카운트다운 종료   = 2026-11-08(일) 08:00 KST
DEADLINE         = 2026-11-23(월) 08:00 KST      (+15일)
dayNum(DEADLINE) = 92
firstDay = 2, lastDay = 91  →  총 90일
```

| k | 시즌 | 기간 | dayNum 구간 | 반영 일수 | 비고 |
|---|---|---|---|---|---|
| 0 | 처녀자리 | 08-23 ~ 09-19 | [0, 27] | **26일** | 이미 2일 지남 |
| 1 | 천칭자리 | 09-20 ~ 10-17 | [28, 55] | **28일** | 온전 |
| 2 | 전갈자리 | 10-18 ~ 11-14 | [56, 83] | **28일** | 온전 |
| 3 | 사수자리 | 11-15 ~ 12-12 | [84, 111] | **8일** | 마감이 시즌 중간 |
| | | | | **합계 90일** ✅ | |

**처녀자리·천칭자리를 2x, 전갈자리·사수자리를 1x로 지정했을 때:**

```
26일 × 210 = 5,460
28일 × 210 = 5,880
28일 × 120 = 3,360
 8일 × 120 =   960
──────────────────
예상 획득    15,660 코인
```

(전부 2x면 18,900 / 전부 1x면 10,800 — 시즌 선택이 실제로 큰 차이를 만든다는 걸 보여주는 좋은 예시라 UI 문구에 써먹어도 된다.)

### 6단계: 상품 판정

상품 목록은 1차본 그대로 상수 배열로 박아둔다 (2026-08 화면 기준):

```js
const SHOP_ITEMS = [
  { name: "동화의 성",          price: 9999 },
  { name: "구름꿈 고래의 노래",  price: 5999 },
  { name: "행운의 올리브",      price: 3999 },
  { name: "크리스마스의 밤",    price: 3999 },
  { name: "가을빛 단풍",        price: 3999 },
  { name: "푸른 서리 그림자",    price: 1666 },
  { name: "동화 왕국",          price: 1666 },
  { name: "수정빛 얼음 꿈결",    price: 999  },
];
```

두 가지를 동시에 보여준다:

**① 개별 판정 (선택과 무관하게 항상)** — 각 상품 행에 배지:
```
사용 가능 코인 ≥ 가격            → 🟢 살 수 있어요
사용 가능 코인 <  가격            → 🔴 N코인 모자라요
```

**② 조합 판정 (체크한 상품이 있을 때)**:
```
선택 합계 S = Σ 선택한 상품 가격
S ≤ 사용가능    → 🟢 전부 살 수 있어요 (남는 코인 사용가능 − S)
S >  사용가능   → 🔴 (S − 사용가능)코인 모자라요
```

**부족할 때는 "얼마나 모자란지"에서 멈추지 말고 행동 가능한 정보로 바꿔줄 것:**

```
🔴 2,340코인 모자라요.
   → 1x로 잡은 전갈자리를 2x로 바꾸면 +2,520코인이라 충분해요.
```

`1x → 2x` 전환 1개당 이득은 `days[k] × (7−4) × 하루상한`으로 즉시 계산된다. **1x로 잡힌 시즌 중 이득이 큰 순서로 훑어서, 부족분을 메울 수 있는 최소 개수의 전환을 제안**하면 된다. 이게 이 도구에서 제일 실용적인 출력이다 — "이번 상점 지르려면 몇 시즌이나 빡세게 굴려야 하나"에 바로 답한다.

---

## UI 구성

전체 1페이지, 위→아래. **모바일 390px 확인 필수.** 색/폰트/카드 스타일은 `CLAUDE.md` 스타일 가이드와 1차본 CSS 그대로.

### ① 안내 박스 (최상단)

```
이 계산기는 "하루 평균 몇 코인"이라는 어림 요율로 계산해요.
슬롯·배치 단위 정밀 계산은 [스타소울 파견 계산기]가 따로 합니다.
```
→ 파견 계산기로 링크. 두 도구를 헷갈리지 않게 하는 게 목적.

### ② 상점 마감 입력

```
┌────────────────────────────────────────┐
│  상점 카운트다운                         │
│  게임 상점 화면 상단 숫자를 그대로 입력하세요 │
│                                         │
│   [ 75 ] 일   [ 22 ] 시간                │
│                                         │
│  게임 카운트다운 종료   11/8(일) 08:00     │
│  유예 기간             + 15일             │
│  ─────────────────────────────────────  │
│  진짜 마감   11/23(월) 08:00 KST          │
│              90일 21:59:31 남음           │
│                                         │
│  ℹ️ 카운트다운이 0이 돼도 15일 동안은        │
│     이 상점에서 계속 살 수 있어요.          │
└────────────────────────────────────────┘
```

- 라이브 카운트다운 `setInterval` 1초 (`fmtDuration` 재사용).
- 재방문 시 저장된 마감 시각 표시 + **"카운트다운 다시 입력"** 버튼.
- **마감이 지났으면** 입력부를 접고 "이 상점은 마감됐어요. 새 카운트다운을 입력해 주세요." 상태로.

### ③ 획득 조건

- **획득 경로 토글**: `무료 12/일` ↔ `패스권 30/일` (기본 패스권)
- **현재 보유 코인** (숫자 입력, 기본 0)
- **`오늘 하루치 아직 남았어요`** 체크박스 (기본 꺼짐) — 문구: "오늘 상한을 아직 안 채웠으면 켜세요"
- 고급 설정(접힘): **소울당 평균 코인 직접 수정** (`2x: 7` / `1x: 4`) — → 열린 질문 1
- 전제 안내:
  > 소울이 생기는 대로 바로 파견하는 기준이에요 (쟁여두기 최적화 미반영).
  > 친구 소울은 포함하지 않았고, 매일 획득 상한을 다 채운다고 가정한 **이론 최대치**입니다.
  > 슬롯이 2개뿐이면 이보다 적게 나올 수 있어요.

### ④ 시즌별 운영 방식 — **이 도구의 핵심 입력**

```
┌────────────────────────────────────────┐
│  시즌별로 어떻게 굴릴 건가요?              │
│  보너스 주에 맞춰 등급을 갈아타면 2x,       │
│  신경 안 쓰고 SSR만 파면 1x예요.           │
│                        [전체 2x][전체 1x] │
│                                         │
│  처녀자리   8/23~9/19                    │
│  26일 반영 (이미 2일 지남)   [ 2x ][ 1x ] │
│  ──────────────────────────────────────  │
│  천칭자리   9/20~10/17                   │
│  28일 반영                  [ 2x ][ 1x ] │
│  ──────────────────────────────────────  │
│  전갈자리   10/18~11/14                  │
│  28일 반영                  [ 2x ][ 1x ] │
│  ──────────────────────────────────────  │
│  사수자리   11/15~12/12                  │
│  8일 반영 (마감이 시즌 중간)  [ 2x ][ 1x ] │
│                                         │
│  ℹ️ 9/20 이후 시즌 이름은 별자리 순서로      │
│     계산한 값이에요.                       │
└────────────────────────────────────────┘
```

- 각 행 우측에 **그 시즌이 기여하는 코인**을 실시간 표시하면 선택 감각이 확 산다 (`26일 × 210 = 5,460코인`).
- 1x 행에는 **"2x로 바꾸면 +N코인"** 힌트를 회색 작은 글씨로.
- 마감 시각이 바뀌면 목록을 다시 만들되, **선택값은 시즌 시작일(YMD)을 키로 보존**한다. 인덱스를 키로 쓰면 마감을 고칠 때마다 선택이 엉뚱한 시즌으로 밀린다.

### ⑤ 결과

```
┌────────────────────────────────────────┐
│  마감까지 90일 · 4개 시즌                 │
│                                         │
│  현재 보유 코인             1,200 코인    │
│  예상 획득 코인            15,660 코인    │
│  ────────────────────────────────────   │
│  사용 가능 코인            16,860 코인    │
│                                         │
│  ▸ 시즌별 내역 (접힘)                     │
│      처녀자리  26일 × 210 =  5,460       │
│      천칭자리  28일 × 210 =  5,880       │
│      전갈자리  28일 × 120 =  3,360       │
│      사수자리   8일 × 120 =    960       │
│                                         │
│  ※ 매일 획득 상한을 다 채웠을 때의 이론      │
│    최대치예요. 실제로는 이보다 적습니다.     │
└────────────────────────────────────────┘
```

**"확정"이라는 표현을 쓰지 말 것.** 마감 카운트다운만 확정이고 코인은 전부 이론 최대치다 (파견 설계 문서와 동일한 원칙).

### ⑥ 상품 목록 + 판정

1차본의 `.item-row` / `.verdict-box` 스타일 그대로. 행마다 🟢/🔴 배지 추가, 하단에 조합 판정 + 시즌 전환 제안.

---

## 구현 메모

- **파일**: `cozytools/shop.html` 단일 파일. `index.html`에는 **이미 카드가 있으므로 추가 작업 없음** (아이콘 🏪, `shop.html`).
- **KST 헬퍼는 `starsoul.html`에서 그대로 복사해온다. 새로 짜지 말 것.**
  필요한 것: `KST_OFFSET_MS`, `toKST`, `fromKSTFields`, `pad2`, `kstDateString`, `parseYMD`, `ymdToKSTInstant`, `fmtDuration`, `fmtKSTClock`.
  **필요 없는 것**: `nextRotationBoundary`, `nextMidnightKST`, `daysUntilBoundaryMidnights`, `currentWeekOf` — 전부 주간 로테이션용이라 상점과 무관하다.
  빌드 스텝이 없어서 모듈 공유가 안 되므로 복붙이 정답이지만, **`starsoul.html`이 정본**이라는 주석을 남겨서 나중에 한쪽만 고치는 사고를 막을 것.
- **`dayNum` 헬퍼를 새로 하나 만든다** — 이 도구에만 필요하다:
  ```js
  const ANCHOR_MS = ymdToKSTInstant(ANCHOR_START, 0, 0).getTime();
  function dayNum(t) {
    return Math.floor((ymdToKSTInstant(kstDateString(t), 0, 0).getTime() - ANCHOR_MS) / 86400000);
  }
  ```
  **`(t − ANCHOR_MS) / 86400000`을 그냥 floor 하면 안 된다.** KST 자정으로 먼저 정규화해야 한다 — 안 그러면 하루의 어느 시각이냐에 따라 시즌 경계가 흔들린다. (KST는 서머타임이 없어서 고정 오프셋만 지키면 이 정규화로 충분하다.)
- **`/seasons` API를 호출하지 않는다.** 별자리 이름은 앵커에서 계산하고, 보너스 데이터는 이 모델에 안 쓰인다. 네트워크 의존이 없으니 오프라인에서도 뜨고 로딩 상태 처리도 필요 없다 — 파견 계산기보다 훨씬 단순해진다.
  (`/seasons`의 최신 시즌 `start`로 앵커를 검증하는 건 가능하지만 **v1에선 하지 말 것.** 서버 데이터에는 별자리 이름 필드가 없어서 이름 매핑은 어차피 하드코딩 앵커에 의존하고, 서버 `start`가 28일 배수에서 어긋나면 오히려 조용히 틀려진다. → 열린 질문 8)
- **오프바이원 명세 — 코드 주석에 그대로 박아둘 것:**
  - 시즌 `k` 구간은 **half-open** `[28k, 28k+28)`
  - 날짜 범위는 **양끝 포함(inclusive)** `[firstDay, lastDay]`
  - `lastDay = dayNum(DEADLINE) − 1` — 마감 당일은 무조건 제외
  - `Σ days[k] === lastDay − firstDay + 1` 검산
- **매직 넘버 금지.** `210`/`120`을 코드에 직접 쓰지 말고 항상 `COIN_PER_SOUL[mode] × DAILY_CAP[path]`로 계산할 것. 무료 경로가 조용히 깨지는 걸 막는다.
- **`localStorage` 키** (전부 `shop_` 접두사):

  | 키 | 값 | 비고 |
  |---|---|---|
  | `shop_deadlineMs` | 숫자 (절대 ms) | **일/시간 원본을 저장하지 말 것** |
  | `shop_path` | `"free"` / `"pass"` | |
  | `shop_coins` | 숫자 | 현재 보유 코인 |
  | `shop_includeToday` | `"1"` / `"0"` | |
  | `shop_seasonModes` | `{"2026-08-23":"x2", ...}` | **시작일 YMD가 키** |
  | `shop_selected` | 상품 인덱스 배열 | 1차본과 동일 |

  마감이 지난 `shop_deadlineMs`는 로드 시 폐기. `shop_seasonModes`에서 마감 이후 시즌 항목은 굳이 지울 필요 없다(무시하면 됨).
- **입력 검증**: 일/시간 음수·비정수·빈 값 방어. 시간은 `0~23`으로 클램프(유저가 `48`을 넣어도 `Date` 산술이 알아서 넘겨주긴 하지만, 게임 표기가 절대 24를 안 넘으므로 입력 단계에서 막는 게 낫다). 마감이 1년 넘게 나오면(시즌 13개 이상) 경고 — 오타일 가능성이 높다.
- **성능은 신경 쓸 게 없다.** 시즌 수가 많아야 6개다. 날짜를 하루씩 루프 돌아도 되지만 위의 정수 공식이 더 짧고 검증하기 쉽다.
- **1차본에서 반드시 지울 것**: `GRADE_COIN` / `GRADES` / `BONUS_MULT` / `FRIEND_MAX` / 등급 탭 / 친구 소울 체크박스 / `coinMin ~ coinMax` 범위 표시 / `state.days`. 남겨두면 "왜 등급을 고르는데 결과가 안 바뀌지?" 하는 혼란만 생긴다.

---

## 아직 정할 것 / 열린 질문

1. **소울당 코인(7 / 4)을 유저가 고칠 수 있게 할 것인가?**
   → **권장: 고급 설정(접힘)에 넣되 기본값은 7/4 고정.** 하루 상한(12/30)은 획득 경로 토글로 이미 다뤄지므로, 남는 변수는 소울당 코인뿐이다. 7은 "SSR 보너스 2주 + SR 보너스 2주"라는 관측 패턴에서 나온 값인데, 파견 설계 문서의 경험적 관찰에 따르면 **한 시즌에 SSR 보너스 주가 0~2주로 흔들린다** — SSR 보너스가 한 주뿐이면 평균이 `(8+6+6+6)/4 = 6.5`로 내려간다. 즉 7은 살짝 낙관적이다. **접힌 채로 두되 고칠 수 있게 열어두는 게 정직하다.**
2. **시즌별 2x/1x의 기본값을 뭘로 할 것인가?**
   → 새우님 본인은 2x 플레이어지만, 이 사이트는 커뮤니티 공개용이고 이 도구의 실패 모드는 "살 수 있다고 했는데 못 사는 것"이다. **기본 1x(보수적) + 상단에 `전체 2x` 일괄 버튼**을 권장하지만, 새우님이 "내가 쓸 도구니까 기본 2x"를 원하면 그것도 합리적이다. **결정 필요.**
3. **"이벤트"가 별자리 시즌과 1:1인가?**
   → 새우님이 요구사항에서 쓴 "이벤트"라는 표현이 **28일 별자리 시즌과 정확히 같은 것**이라고 전제하고 설계했다. 근거: 2026-08-23·2026-07-26 두 실측치가 파견 시즌 시작일(`/seasons`의 `id`)과 정확히 일치한다. **다만 게임 안에서 이 28일 주기를 부르는 공식 명칭이 뭔지("○○자리 이벤트"? "시즌"?), 그리고 그것과 별개로 굴러가는 다른 "이벤트"가 있는 건 아닌지 한 번 확인해줄 것.** UI 라벨을 뭘로 쓸지가 여기 달렸다.
4. **유예 기간이 정확히 15일인가?** 달력일 기준인가, 아니면 카운트다운 종료 시각 + 정확히 360시간인가? 새우님 관측치라 상수 하나로 빼뒀다. 그리고 **유예 기간 동안에도 코인 획득이 평소와 똑같이 되는지**(다음 상점이 동시에 열려 있는 상태라도 파견/획득은 영향 없을 것으로 보이지만) 확인 필요.
5. **상점이 닫히면 남은 코인은 어떻게 되나?** 소멸인가, 다음 상점으로 이월인가?
   → **이게 이 도구의 전제 자체를 흔드는 질문이다.** 이월된다면 "마감 전까지 모을 수 있는 양"이라는 프레임이 약해지고(못 사면 다음 상점에서 사면 됨), 소멸이라면 지금 설계가 정확하다. **소멸이라면 결과 카드에 "마감 후 남는 코인은 사라져요" 경고를 넣어야 하고**, 반대로 이월이면 "이번에 못 사도 다음 상점에서 이어서" 안내가 필요하다. **우선 확인.**
6. **상품 목록/가격 운영.** 지금은 상수 배열 하드코딩(2026-08 화면 기준). 상점이 바뀔 때마다 새우님이 코드를 고쳐야 한다. 상점 주기가 100일 이상이라 빈도가 낮으니 v1은 하드코딩으로 충분하지만, **불편하면 `register.html`에 상품 등록창을 추가하는 게 다음 수순**(시즌 꽃 등록창과 같은 패턴). v1 필수 아님.
7. **현재 보유 소울 재고 반영.** 지금 창고에 소울이 쌓여 있으면 그건 곧 코인인데 v1은 무시한다. 넣는다면 "보유 소울 N마리 × 소울당 코인"으로 단순 환산하면 되지만, **등급이 섞여 있으면 환산이 애매하다.** 필요하다는 반응이 나오면 v2에.
8. **별자리 앵커 검증 시점.** `2026-09-20 = 천칭자리`가 맞는지 그날 이후 게임에서 확인해줄 것. 틀리면 `ANCHOR_INDEX` 하나만 고치면 된다.
   덧붙여, **`/seasons` 스키마에 시즌 이름 필드가 없다** — `register.html`에서 시즌 등록할 때 별자리 이름도 같이 받아두면 나중에 "역대 로테이션 기록" 콘텐츠에서 바로 쓸 수 있다. 이 계산기는 계산으로 때우므로 **v1 필수는 아니지만 등록 시 한 칸 늘리는 비용이 거의 없다.**
9. **친구 소울 옵션.** 지금 요율(7/4 × 30)은 친구 도움 없는 기준이다. 친구 소울 4개/일을 챙기는 유저는 실제로 더 번다(`+4 × 소울당코인`/일 ≈ 2x 기준 +28코인/일, 90일이면 +2,520코인 — 무시할 크기가 아니다). **파견 계산기처럼 "추정" 영역으로 분리해서 별도 표시하는 옵션을 v2에 검토.**

---

## 시작할 때 참고

- **`cozytools/CLAUDE.md`와 `스타소울파견계산기-설계.md`를 먼저 읽을 것.** 특히 파견 문서의 "코인/소요시간 표"와 "핵심 계산 결과 1번(등급별 슬롯 효율 동일)"이 이 문서의 요율 7/4가 어디서 나왔는지의 근거다.
- **②번 마감 카드 → ④번 시즌 목록 → ⑤번 결과 순서로 만들 것.** ②④가 완성되면 계산은 곱셈 몇 줄이라 금방 붙는다. 반대로 계산부터 만들면 날짜 경계를 눈으로 검증할 수단이 없어서 오프바이원을 놓친다.
- **위 "워크드 예제"로 반드시 검증할 것.** `now`를 인자로 받을 수 있게 함수를 짜두면(`buildSeasonList(now, deadline)`) 2026-08-24 10:00을 넣어서 `26/28/28/8일`과 `15,660코인`이 나오는지 바로 확인할 수 있다. **`new Date()`를 함수 안에서 직접 부르면 테스트가 불가능해진다.**
- **기존 `shop.html`의 CSS·상품 목록·`localStorage` 패턴은 재사용하고, `<script>` 계산부만 갈아끼우는 게 가장 빠르다.**
- 배포 후 `backlog.md`(및 PC의 `TODO.md`) cozytools 절에 기록을 남기고, `스타소울파견계산기-설계.md`의 열린 질문 6번을 "해결됨 → `스타소울상점계산기-설계.md`"로 갱신할 것.
